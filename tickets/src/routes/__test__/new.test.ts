import request from "supertest";
import { app } from "../../app";

it("can only be accessed if the user is signed in", async () => {});
it("does not create a new ticket for an unauthenticated user", async () => {});

it("does not create duplicate tickets with invalid price", async () => {});
